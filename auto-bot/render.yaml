services:
  - type: web
    name: dylansautosales_facebook_tool
    runtime: python
    buildCommand: pip install -r auto-bot/api/requirements.txt
    startCommand: uvicorn auto-bot.api.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
